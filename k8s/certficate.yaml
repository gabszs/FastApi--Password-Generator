apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portfile
  namespace: password-generator
spec:
  secretName: portfile-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "password-svc.portfile.dev"
  dnsNames:
  - "password-svc.portfile.dev"

