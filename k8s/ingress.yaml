apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: password-generator-ingress
  namespace: password-generator
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`www.password-svc.portfile.dev`)
      kind: Rule
      services:
        - name: password-generator-svc
          port: 80
    - match: Host(`password-svc.portfile.dev`)
      kind: Rule
      services:
        - name: password-generator-svc
          port: 80
      middlewares:
        - name: default-headers
  tls:
    secretName: portfile-tls



# ---

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: nginx-ingress
#   namespace: test-dploy  # Definindo o namespace
#   annotations:
#     cert-manager.io/cluster-issuer: "letsencrypt-prod"  # Usando o cluster issuer do cert-manager
# spec:
#   rules:
#     - host: your.domain.portfile.dev  # Substitua pelo seu domínio
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: nginx-service  # Nome do serviço
#                 port:
#                   number: 80
#   tls:
#     - hosts:
#         - nginx-test.portfile.dev  # Substitua pelo seu domínio
#       secretName: nginx-tls  #
